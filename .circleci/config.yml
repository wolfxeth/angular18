# This config was automatically generated from your source code
# Stacks detected: deps:node:angular18
version: 2.1
orbs:
  node: circleci/node@5
jobs:
  test-node:
    docker:
      - image: cimg/node:20.18-browsers
    working_directory: ~/project/angular18
    steps:
      - checkout:
          path: ~/project
      - node/install-packages:
          pkg-manager: npm
      - run:
          name: Run tests
          command: npm test -- --watch=false --browsers=ChromeHeadless
  build-node:
    # Build node project
    executor: node/default
    working_directory: ~/project/angular18
    steps:
      - checkout:
          path: ~/project
      - node/install-packages:
          pkg-manager: npm
      - run:
          command: npm run build
      - run:
          name: Create the ~/artifacts directory if it doesn't exist
          command: mkdir -p ~/artifacts
      # Copy output to artifacts dir
      - run:
          name: Copy artifacts
          command: cp -R build dist public .output .next .docusaurus ~/artifacts 2>/dev/null || true
      - store_artifacts:
          path: ~/artifacts
          destination: node-build
  deploy:
    # This is an example deploy job, not actually used by the workflow
    docker:
      - image: cimg/base:stable
    steps:
      # Replace this with steps to deploy to users
      - run:
          name: deploy
          command: '#e.g. ./deploy.sh'
workflows:
  build-and-test:
    jobs:
      # Temporarily disabled until the CI executor can launch Chrome Headless.
      # - test-node
      - build-node
    # - deploy:
    #     requires:
    #       - build-node
